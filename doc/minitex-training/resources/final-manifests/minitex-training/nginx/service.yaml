apiVersion: v1
kind: Service
metadata:
  name: nginx
  namespace: minitex-training
spec:
  selector:
    app: nginx
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  type: ClusterIP
